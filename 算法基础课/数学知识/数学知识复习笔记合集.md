# 📚 数学知识复习笔记合集

## 目录

1. [质数与筛法](#1-质数与筛法)
2. [质因数分解](#2-质因数分解)
3. [约数与欧拉函数](#3-约数与欧拉函数)
4. [快速幂与逆元](#4-快速幂与逆元)
5. [扩展欧几里得与同余方程](#5-扩展欧几里得与同余方程)
6. [高斯消元](#6-高斯消元)
7. [组合数学](#7-组合数学)
8. [容斥原理](#8-容斥原理)
9. [博弈论 (SG函数)](#9-博弈论-sg函数)

---

## 1. 质数与筛法

### 1.1 算法概述与选型指南

**核心思想**：高效筛选出 $1 \sim N$ 中的所有质数。

| 算法 | 时间复杂度 | 空间复杂度 | 适用场景 |
|:---:|:---:|:---:|:---:|
| **埃氏筛** | $O(N \log \log N)$ | $O(N)$ | $N \le 10^7$ |
| **线性筛 (欧拉筛)** | $O(N)$ | $O(N)$ | $N \le 10^7$，竞赛首选 |
| **试除法判质数** | $O(\sqrt{N})$ | $O(1)$ | 单次查询、$N \le 10^{12}$ |

### 1.2 核心模板

**线性筛 (欧拉筛)**：
```cpp
const int N = 1e6 + 10;
int primes[N], cnt;      // primes存储质数，cnt为质数个数
bool st[N];              // st[i] = true 表示i被筛掉

void get_primes(int n) {
    for (int i = 2; i <= n; i++) {
        if (!st[i]) primes[cnt++] = i;  // i是质数
        for (int j = 0; primes[j] <= n / i; j++) {
            st[primes[j] * i] = true;    // 筛掉合数
            if (i % primes[j] == 0) break; // 关键：保证每个数只被最小质因子筛
        }
    }
}
```

**试除法判质数**：
```cpp
bool is_prime(int x) {
    if (x < 2) return false;
    for (int i = 2; i <= x / i; i++)  // i * i <= x 防溢出写法
        if (x % i == 0) return false;
    return true;
}
```

### 1.3 进阶技巧与模型变形

- **区间筛**：求 $[L, R]$ 区间质数（$R \le 10^{12}, R-L \le 10^6$），先筛 $\sqrt{R}$ 内质数再标记区间
- **质数密度**：$1 \sim N$ 中约有 $N / \ln N$ 个质数，用于估算数组大小
- **Miller-Rabin**：大数判质数（$N \le 10^{18}$），基于费马小定理的随机化算法
- **Pollard-Rho**：大数质因数分解

### 1.4 ⚠️ 细节与致命坑点

- **数组大小**：`primes` 数组只需开到 $N / \ln N$，约 $N/10$ 足够
- **循环条件**：`primes[j] <= n / i` 避免 `primes[j] * i` 溢出
- **break 位置**：`if (i % primes[j] == 0) break` 必须在内层循环内，保证线性复杂度
- **从2开始**：质数定义从 $2$ 开始，$1$ 不是质数

---

## 2. 质因数分解

### 2.1 算法概述与选型指南

**核心思想**：将一个数分解为若干质数的幂次乘积 $N = p_1^{a_1} \cdot p_2^{a_2} \cdots p_k^{a_k}$。

| 方法 | 时间复杂度 | 适用场景 |
|:---:|:---:|:---:|
| **试除法** | $O(\sqrt{N})$ | $N \le 10^{12}$，单次数分解 |
| **预处理最小质因子** | $O(\log N)$ 单次 | 多次查询、$N \le 10^7$ |

### 2.2 核心模板

**试除法分解质因数**：
```cpp
// 返回vector of (质因子, 指数)
vector<pair<int, int>> divide(int x) {
    vector<pair<int, int>> res;
    for (int i = 2; i <= x / i; i++) {
        if (x % i == 0) {
            int cnt = 0;
            while (x % i == 0) {
                x /= i;
                cnt++;
            }
            res.push_back({i, cnt});
        }
    }
    if (x > 1) res.push_back({x, 1});  // 剩余的大于1的因子
    return res;
}
```

### 2.3 进阶技巧与模型变形

- **阶乘分解**：$n!$ 中质数 $p$ 的次数为 $\sum_{k=1}^{\infty} \lfloor n / p^k \rfloor$
- **组合数质因数分解**：$C_n^m = n! / (m! \cdot (n-m)!)$，分别分解后指数相减
- **积性函数**：利用质因数分解求欧拉函数、约数个数等

### 2.4 ⚠️ 细节与致命坑点

- **最后判断**：循环结束后必须判断 `if (x > 1)`，此时 $x$ 是大于 $\sqrt{N}$ 的质因子
- **重复质因子**：必须用 `while` 统计次数，不能只用 `if`
- **$x / i$ vs $i * i$**：为避免溢出，用 `i <= x / i` 而非 `i * i <= x`

---

## 3. 约数与欧拉函数

### 3.1 算法概述与选型指南

**核心思想**：
- **约数个数**：若 $N = \prod p_i^{a_i}$，则 $d(N) = \prod (a_i + 1)$
- **约数之和**：$\sigma(N) = \prod \frac{p_i^{a_i+1} - 1}{p_i - 1}$
- **欧拉函数**：$\varphi(N)$ 表示 $1 \sim N$ 中与 $N$ 互质的数的个数

| 算法 | 时间复杂度 | 适用场景 |
|:---:|:---:|:---:|
| **试除法求约数** | $O(\sqrt{N})$ | 单次查询 |
| **质因数分解求约数个数/和** | $O(\sqrt{N})$ | 大范围求积性函数 |
| **线性筛求欧拉函数** | $O(N)$ | 预处理 $1 \sim N$ 所有 $\varphi(i)$ |

### 3.2 核心模板

**试除法求所有约数**：
```cpp
vector<int> get_divisors(int x) {
    vector<int> res;
    for (int i = 1; i <= x / i; i++) {
        if (x % i == 0) {
            res.push_back(i);
            if (i != x / i) res.push_back(x / i);  // 避免重复
        }
    }
    sort(res.begin(), res.end());
    return res;
}
```

**欧拉函数 (单次)**：
```cpp
int phi(int x) {
    int res = x;
    for (int i = 2; i <= x / i; i++) {
        if (x % i == 0) {
            res = res / i * (i - 1);  // 先除后乘防溢出
            while (x % i == 0) x /= i;
        }
    }
    if (x > 1) res = res / x * (x - 1);
    return res;
}
```

**线性筛求欧拉函数 (预处理)**：
```cpp
int primes[N], cnt;
bool st[N];
int euler[N];

void get_euler(int n) {
    euler[1] = 1;  // 关键初始化
    for (int i = 2; i <= n; i++) {
        if (!st[i]) {
            primes[cnt++] = i;
            euler[i] = i - 1;  // 质数的欧拉函数
        }
        for (int j = 0; primes[j] <= n / i; j++) {
            st[primes[j] * i] = true;
            if (i % primes[j] == 0) {
                // primes[j]是i的最小质因子
                euler[primes[j] * i] = euler[i] * primes[j];
                break;
            }
            euler[primes[j] * i] = euler[i] * (primes[j] - 1);
        }
    }
}
```

**约数个数与约数之和**：
```cpp
// 分解质因数后计算
unordered_map<int, int> primes;  // (质因子, 指数)

// 约数个数
divisor_count = 1;
for (auto &[p, a] : primes)
    divisor_count = divisor_count * (a + 1) % mod;

// 约数之和
divisor_sum = 1;
for (auto &[p, a] : primes) {
    long long t = 1;
    while (a--) t = (t * p + 1) % mod;  // 等比数列求和递推
    divisor_sum = divisor_sum * t % mod;
}
```

### 3.3 进阶技巧与模型变形

- **欧拉定理**：若 $\gcd(a, n) = 1$，则 $a^{\varphi(n)} \equiv 1 \pmod{n}$
- **费马小定理**：$p$ 为质数时，$a^{p-1} \equiv 1 \pmod{p}$
- **欧拉函数求和**：$\sum_{i=1}^{n} \varphi(i)$ 可以用线性筛预处理
- **莫比乌斯反演**：与约数、欧拉函数相关的复杂计数问题

### 3.4 ⚠️ 细节与致命坑点

- **euler[1] = 1**：线性筛时必须初始化
- **公式记忆**：
  - 约数个数：$(a_1+1)(a_2+1)\cdots$
  - 约数之和：几何级数乘积
- **long long**：多个数相乘取模时，中间结果可能溢出 `int`
- **先除后乘**：`res = res / i * (i-1)` 避免浮点数

---

## 4. 快速幂与逆元

### 4.1 算法概述与选型指南

**核心思想**：利用二进制拆分，$O(\log k)$ 计算 $a^k \mod p$。

| 算法 | 时间复杂度 | 适用场景 |
|:---:|:---:|:---:|
| **快速幂** | $O(\log k)$ | 大指数模运算 |
| **快速乘** | $O(\log b)$ | 防止 $a \cdot b$ 溢出（$a, b \le 10^{18}$） |
| **费马小定理求逆元** | $O(\log p)$ | $p$ 为质数 |
| **扩展欧几里得求逆元** | $O(\log a)$ | $p$ 非质数，要求 $\gcd(a, p) = 1$ |

### 4.2 核心模板

**快速幂**：
```cpp
long long qmi(long long a, long long k, long long p) {
    long long res = 1 % p;
    a %= p;  // 防止a初始就大于p
    while (k) {
        if (k & 1) res = res * a % p;
        a = a * a % p;
        k >>= 1;
    }
    return res;
}
```

**快速乘 (防溢出)**：
```cpp
long long qmul(long long a, long long b, long long p) {
    long long res = 0;
    while (b) {
        if (b & 1) res = (res + a) % p;
        a = (a + a) % p;
        b >>= 1;
    }
    return res;
}
```

**逆元**：
```cpp
// 费马小定理，p为质数
long long inv(long long a, long long p) {
    return qmi(a, p - 2, p);
}

// 扩展欧几里得，p可以非质数但要求gcd(a,p)=1
long long inv_exgcd(long long a, long long p) {
    long long x, y;
    long long d = exgcd(a, p, x, y);
    if (d != 1) return -1;  // 逆元不存在
    return (x % p + p) % p;
}
```

### 4.3 进阶技巧与模型变形

- **线性求逆元**：预处理 $1 \sim n$ 在模 $p$ 下的逆元
  ```cpp
  inv[1] = 1;
  for (int i = 2; i <= n; i++)
      inv[i] = (p - p / i) * inv[p % i] % p;
  ```
- **矩阵快速幂**：求线性递推数列（如斐波那契）
- **倍增思想**：快速幂的思想可推广到其他满足结合律的运算

### 4.4 ⚠️ 细节与致命坑点

- **初始化**：`res = 1 % p` 防止 $p = 1$ 时出错
- **取模时机**：每次乘法后都取模，防止溢出
- **负数处理**：逆元计算后调整至 $[0, p-1]$ 范围
- **逆元存在条件**：必须 $\gcd(a, p) = 1$

---

## 5. 扩展欧几里得与同余方程

### 5.1 算法概述与选型指南

**核心思想**：求解 $ax + by = \gcd(a, b)$ 的整数解，进而解线性同余方程。

| 问题 | 解法 | 时间复杂度 |
|:---:|:---:|:---:|
| **exgcd** | 扩展欧几里得 | $O(\log \min(a,b))$ |
| **线性同余方程** $ax \equiv b \pmod{m}$ | exgcd转化 | $O(\log m)$ |
| **中国剩余定理 (CRT)** | 合并同余方程 | $O(n \log M)$ |
| **扩展CRT** | 模数不互质时 | $O(n \log M)$ |

### 5.2 核心模板

**扩展欧几里得**：
```cpp
// 返回gcd(a,b)，同时求出ax + by = gcd(a,b)的一组解
int exgcd(int a, int b, int &x, int &y) {
    if (!b) {
        x = 1, y = 0;
        return a;
    }
    int d = exgcd(b, a % b, y, x);  // 注意x,y交换位置
    y -= a / b * x;
    return d;
}
```

**线性同余方程** $ax \equiv b \pmod{m}$：
```cpp
// 求解 ax ≡ b (mod m)
// 等价于 ax + my = b
int linear_congruence(int a, int b, int m) {
    int x, y;
    int d = exgcd(a, m, x, y);
    if (b % d) return -1;  // 无解
    x = (long long)b / d * x % m;  // 特解
    // 通解：x + k * (m/d)，最小正整数解：
    return (x % (m / d) + m / d) % (m / d);
}
```

**扩展中国剩余定理 (模数不互质)**：
```cpp
typedef long long LL;

LL exgcd(LL a, LL b, LL &x, LL &y) {
    if (!b) { x = 1, y = 0; return a; }
    LL d = exgcd(b, a % b, y, x);
    y -= a / b * x;
    return d;
}

// 合并 x ≡ m1 (mod a1) 和 x ≡ m2 (mod a2)
// 返回 (新的a, 新的m)，无解返回 (-1, -1)
pair<LL, LL> merge(LL a1, LL m1, LL a2, LL m2) {
    LL k1, k2;
    LL d = exgcd(a1, a2, k1, k2);
    if ((m2 - m1) % d) return {-1, -1};  // 无解
    
    k1 *= (m2 - m1) / d;
    LL t = a2 / d;
    k1 = (k1 % t + t) % t;  // 调整k1为最小正整数
    
    m1 = a1 * k1 + m1;      // 新的余数
    a1 = abs(a1 / d * a2);  // 新的模数 (lcm)
    m1 = (m1 % a1 + a1) % a1;
    
    return {a1, m1};
}

// 主函数中迭代合并n个同余方程
LL crt(vector<LL> &a, vector<LL> &m, int n) {
    LL A = a[0], M = m[0];
    for (int i = 1; i < n; i++) {
        auto [newA, newM] = merge(A, M, a[i], m[i]);
        if (newA == -1) return -1;  // 无解
        A = newA, M = newM;
    }
    return M;
}
```

### 5.3 进阶技巧与模型变形

- **多组同余方程**：用扩展CRT逐个合并
- **不定方程求非负解**：先求特解，再调整参数范围
- **青蛙约会问题**：转化为同余方程 $x + km \equiv y + kn \pmod{L}$

### 5.4 ⚠️ 细节与致命坑点

- **exgcd参数顺序**：递归调用时 `y, x` 交换，这是关键
- **解的调整**：`x = (x % m + m) % m` 确保非负
- **合并时的符号**：注意 $(m_2 - m_1)$ 可能为负
- **long long**：CRT 合并时模数可能很快溢出 `int`
- **最小正整数解**：通解为 $x_0 + k \cdot \frac{m}{d}$，注意区间

---

## 6. 高斯消元

### 6.1 算法概述与选型指南

**核心思想**：通过初等行变换将增广矩阵化为行阶梯形，求解线性方程组。

| 类型 | 时间复杂度 | 适用场景 |
|:---:|:---:|:---:|
| **实数高斯消元** | $O(n^3)$ | 浮点数方程组 |
| **异或高斯消元** | $O(n^3 / w)$ 或 $O(n^3)$ | 模2方程组 |

### 6.2 核心模板

**实数高斯消元**：
```cpp
const int N = 110;
const double eps = 1e-8;
double a[N][N];  // 增广矩阵，最后一列为常数项

// 返回值：0无解，1无穷多解，2唯一解
int gauss(int n) {
    int c, r;
    for (c = 0, r = 0; c < n; c++) {
        // 找主元（绝对值最大）
        int t = r;
        for (int i = r; i < n; i++)
            if (fabs(a[i][c]) > fabs(a[t][c]))
                t = i;
        
        if (fabs(a[t][c]) < eps) continue;  // 该列全为0
        
        // 交换到第r行
        for (int i = c; i <= n; i++) swap(a[r][i], a[t][i]);
        
        // 归一化
        for (int i = n; i >= c; i--) a[r][i] /= a[r][c];
        
        // 消去下方行
        for (int i = r + 1; i < n; i++)
            if (fabs(a[i][c]) > eps)
                for (int j = n; j >= c; j--)
                    a[i][j] -= a[r][j] * a[i][c];
        
        r++;
    }
    
    // 判断无解
    for (int i = r; i < n; i++)
        if (fabs(a[i][n]) > eps) return 0;
    
    // 判断无穷多解（存在自由变量）
    if (r < n) return 1;
    
    // 回代求解
    for (int i = n - 1; i >= 0; i--)
        for (int j = i + 1; j < n; j++)
            a[i][n] -= a[j][n] * a[i][j];
    
    return 2;  // 唯一解在a[i][n]中
}
```

**异或高斯消元 (模2)**：
```cpp
const int N = 110;
int a[N][N];  // 0或1的矩阵

int gauss_xor(int n) {
    int c, r;
    for (c = 0, r = 0; c < n; c++) {
        // 找主元（该列为1的行）
        int t = r;
        for (int i = r; i < n; i++)
            if (a[i][c]) { t = i; break; }
        
        if (!a[t][c]) continue;  // 该列全为0
        
        // 交换
        for (int i = c; i <= n; i++) swap(a[r][i], a[t][i]);
        
        // 消去（异或运算）
        for (int i = r + 1; i < n; i++)
            if (a[i][c])
                for (int j = c; j <= n; j++)
                    a[i][j] ^= a[r][j];
        
        r++;
    }
    
    // 判断无解：0 = 1
    for (int i = r; i < n; i++)
        if (a[i][n]) return 0;
    
    if (r < n) return 1;  // 无穷多解
    
    // 回代
    for (int i = n - 1; i >= 0; i--)
        for (int j = i + 1; j < n; j++)
            a[i][n] ^= a[i][j] & a[j][n];
    
    return 2;
}
```

### 6.3 进阶技巧与模型变形

- **求矩阵的秩**：消元后非零行数
- **求行列式**：消元后对角线乘积，注意交换行的符号
- **求逆矩阵**：$[A|I]$ 消元为 $[I|A^{-1}]$
- **开关灯问题**：异或高斯消元经典应用
- **自由变量处理**：无穷多解时，自由变量可任取

### 6.4 ⚠️ 细节与致命坑点

- **eps精度**：浮点数比较时用 `fabs(x) < eps`，通常 $eps = 1e-8$
- **绝对值最大主元**：提高数值稳定性，避免除以小数
- **回代顺序**：从下往上回代
- **列循环 vs 行循环**：外层循环是列 `c`，内层找主元
- **解的输出**：唯一解时解为 `a[i][n]`（最后一列）

---

## 7. 组合数学

### 7.1 算法概述与选型指南

**核心思想**：计算组合数 $C_n^m = \frac{n!}{m!(n-m)!}$ 在不同数据范围下的方法。

| 方法 | 时间复杂度 | 适用数据范围 | 特点 |
|:---:|:---:|:---:|:---:|
| **递推法 (杨辉三角)** | $O(N^2)$ 预处理 | $n \le 2000$ | 可模任意数 |
| **阶乘+逆元** | $O(N)$ 预处理 | $n \le 10^5$，模质数 | 单次 $O(1)$ |
| **Lucas定理** | $O(p \log_p n)$ | $n \le 10^{18}$，$p$ 小 | 大组合数模质数 |
| **分解质因数** | $O(\sqrt{n})$ | 不取模或高精度 | 精确值 |

### 7.2 核心模板

**递推法 (杨辉三角)**：
```cpp
const int N = 2010, mod = 1e9 + 7;
int c[N][N];

void init() {
    for (int i = 0; i < N; i++)
        for (int j = 0; j <= i; j++)
            if (!j) c[i][j] = 1;
            else c[i][j] = (c[i-1][j] + c[i-1][j-1]) % mod;
}
```

**阶乘+逆元法**：
```cpp
const int N = 1e5 + 10, mod = 1e9 + 7;
int fact[N], infact[N];

int qmi(int a, int k) {
    int res = 1;
    while (k) {
        if (k & 1) res = (long long)res * a % mod;
        a = (long long)a * a % mod;
        k >>= 1;
    }
    return res;
}

void init() {
    fact[0] = infact[0] = 1;
    for (int i = 1; i < N; i++) {
        fact[i] = (long long)fact[i-1] * i % mod;
        infact[i] = (long long)infact[i-1] * qmi(i, mod-2) % mod;
    }
}

int C(int n, int m) {
    if (m < 0 || m > n) return 0;
    return (long long)fact[n] * infact[m] % mod * infact[n-m] % mod;
}
```

**Lucas定理**：
```cpp
int C(int a, int b, int p) {  // 小组合数模p
    if (b > a) return 0;
    int res = 1;
    for (int i = 1, j = a; i <= b; i++, j--) {
        res = (long long)res * j % p;
        res = (long long)res * qmi(i, p-2, p) % p;
    }
    return res;
}

int lucas(long long a, long long b, int p) {
    if (a < p && b < p) return C(a, b, p);
    return (long long)C(a % p, b % p, p) * lucas(a / p, b / p, p) % p;
}
```

**高精度组合数 (分解质因数)**：
```cpp
// 计算n!中质数p的指数
int get(int n, int p) {
    int res = 0;
    while (n) {
        res += n / p;
        n /= p;
    }
    return res;
}

// 高精度乘法
vector<int> mul(vector<int> a, int b) {
    vector<int> c;
    for (int i = 0, t = 0; i < a.size() || t; i++) {
        if (i < a.size()) t += a[i] * b;
        c.push_back(t % 10);
        t /= 10;
    }
    while (c.size() > 1 && c.back() == 0) c.pop_back();
    return c;
}

// 主函数：筛质数后，对每个质数计算指数，然后高精度乘起来
```

### 7.3 进阶技巧与模型变形

- **卡特兰数**：$Cat_n = \frac{1}{n+1}C_{2n}^{n} = C_{2n}^{n} - C_{2n}^{n+1}$，应用：合法括号序列、二叉树计数、不交叉路径
- **隔板法**：正整数解个数 $C_{n-1}^{m-1}$，非负整数解 $C_{n+m-1}^{m-1}$
- **二项式定理**：$(a+b)^n = \sum_{k=0}^{n} C_n^k a^k b^{n-k}$
- **组合数前缀和**：$\sum_{i=0}^{m} C_n^i$ 可用递推或FFT加速

### 7.4 ⚠️ 细节与致命坑点

- **预处理范围**：阶乘数组要开到最大可能用到的 $n$
- **逆元预处理**：`infact[i] = fact[i]^(mod-2)`，用快速幂或线性递推
- **Lucas递归边界**：`a < p && b < p` 时直接计算小组合数
- **m > n 判断**：返回 0，避免数组越界
- **卡特兰数取模**：$Cat_n = C_{2n}^{n} \cdot inv(n+1) \% mod$

---

## 8. 容斥原理

### 8.1 算法概述与选型指南

**核心思想**：计算多个集合的并集大小，通过奇加偶减避免重复计数。

| 形式 | 公式 | 适用场景 |
|:---:|:---:|:---:|
| **两集合** | $|A \cup B| = |A| + |B| - |A \cap B|$ | 基础应用 |
| **n集合** | $\sum |A_i| - \sum |A_i \cap A_j| + \sum |A_i \cap A_j \cap A_k| - \cdots$ | 一般情况 |
| **二进制枚举** | $O(2^n)$ 枚举子集 | $n \le 20$ |

### 8.2 核心模板

**容斥原理 (能被整除的数)**：
```cpp
// 求1~n中能被p[0..m-1]中至少一个数整除的数的个数
int n, m, p[20];

int res = 0;
for (int i = 1; i < (1 << m); i++) {  // 枚举非空子集
    int t = 1, cnt = 0;  // t为lcm，cnt为选中集合数
    for (int j = 0; j < m; j++) {
        if (i >> j & 1) {
            cnt++;
            // 避免溢出，或lcm超过n时可直接break
            if ((long long)t * p[j] > n) {
                t = -1;
                break;
            }
            t *= p[j];  // 假设p中两两互质，否则用lcm
        }
    }
    if (t == -1) continue;
    // 奇加偶减
    if (cnt & 1) res += n / t;
    else res -= n / t;
}
```

### 8.3 进阶技巧与模型变形

- **求交集而非并集**：转化为全集减并集
- **错排问题**：$D_n = n! \sum_{k=0}^{n} \frac{(-1)^k}{k!}$
- **莫比乌斯函数**：容斥的预处理形式，$\mu(d)$ 表示 $d$ 的质因子个数的奇偶性
- **狄利克雷卷积**：容斥原理的高级形式

### 8.4 ⚠️ 细节与致命坑点

- **从1开始枚举**：`i = 1` 到 `(1 << m) - 1`，空集不计入
- **奇偶判断**：`cnt & 1` 判断集合个数奇偶性
- **LCM溢出**：计算前先判断是否会超过范围
- **互质假设**：若不互质需用 `t = lcm(t, p[j]) = t / gcd(t, p[j]) * p[j]`

---

## 9. 博弈论 (SG函数)

### 9.1 算法概述与选型指南

**核心思想**：
- **Nim游戏**：异或和为0则必败，否则必胜
- **SG函数**：$sg(x) = \text{mex}\{sg(y) \mid y \text{是}x\text{的后继状态}\}$
- **Sprague-Grundy定理**：多堆游戏的SG值为各堆SG值的异或

| 游戏类型 | SG函数/解法 | 特点 |
|:---:|:---:|:---:|
| **Nim游戏** | 所有堆异或和 | 经典基础 |
| **台阶Nim** | 奇数台阶异或和 | 偶数台阶不影响 |
| **集合Nim** | 记忆化搜索求SG | 可选操作给定 |
| **拆分Nim** | $sg(x) = \text{mex}\{sg(i) \oplus sg(j)\}$ | 一堆拆两堆 |

### 9.2 核心模板

**Nim游戏**：
```cpp
int res = 0;
for (int i = 0; i < n; i++) {
    int x; cin >> x;
    res ^= x;  // 异或所有堆
}
if (res) puts("Yes");  // 先手必胜
else puts("No");       // 先手必败
```

**台阶Nim游戏**：
```cpp
int res = 0;
for (int i = 1; i <= n; i++) {
    int x; cin >> x;
    if (i & 1) res ^= x;  // 只异或奇数台阶
}
if (res) puts("Yes");
else puts("No");
```

**记忆化搜索求SG函数**：
```cpp
const int N = 110, M = 1e4 + 10;
int s[N];        // 可选的操作集合
int f[M];        // 记忆化数组，-1表示未计算

int sg(int x) {
    if (f[x] != -1) return f[x];
    
    unordered_set<int> S;
    for (int i = 0; i < k; i++) {
        if (x >= s[i])
            S.insert(sg(x - s[i]));
    }
    
    // mex: 最小的非负整数不在S中
    for (int i = 0; ; i++)
        if (!S.count(i))
            return f[x] = i;
}

// 主函数
memset(f, -1, sizeof f);
int res = 0;
for (int i = 0; i < n; i++) {
    int x; cin >> x;
    res ^= sg(x);
}
if (res) puts("Yes");
else puts("No");
```

**拆分Nim游戏**（一堆拆成两堆）：
```cpp
int sg(int x) {
    if (f[x] != -1) return f[x];
    
    unordered_set<int> S;
    // 枚举所有拆分方式 (i, j)，其中 i + j = x，i >= j
    for (int i = 0; i < x; i++)
        for (int j = 0; j <= i; j++)
            S.insert(sg(i) ^ sg(j));
    
    for (int i = 0; ; i++)
        if (!S.count(i))
            return f[x] = i;
}
```

### 9.3 进阶技巧与模型变形

- **Nim和与异或**：Nim游戏的SG值就是堆大小本身
- **Anti-Nim**：最后取石子者输，判断条件更复杂
- **威佐夫博弈 (Wythoff)**：两堆石子，可取任意一堆或同时取相同数量，必败态为 $(\lfloor k\phi \rfloor, \lfloor k\phi^2 \rfloor)$，$\phi = \frac{1+\sqrt{5}}{2}$
- **Fibonacci博弈**：一堆石子，先手不能全部取完，之后不能超过上一次取的2倍，必败态为Fibonacci数
- **Green Hackenbush**：图论博弈，可用SG函数分析

### 9.4 ⚠️ 细节与致命坑点

- **记忆化初始化**：`memset(f, -1, sizeof f)` 必须在多组数据前执行
- **mex计算**：用 `unordered_set` 或数组标记，注意清空
- **SG范围**：$sg(x)$ 的值通常不大，但要用足够大的数组
- **拆分Nim的枚举**：注意 $i + j = x$ 且避免重复拆分
- **多堆游戏**：最终判断是各堆SG值的异或和

---

*笔记整理完成，祝竞赛/面试顺利！*
